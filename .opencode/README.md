# OpenCode / OpenClaw Adapter

OpenCode and OpenClaw-style installer support is planned.

For now, use the canonical skill source in `../skills/` and map installer metadata from `../manifests/skills.json`. Do not fork the skill text here.
